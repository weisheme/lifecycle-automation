query TagByName($owner: String!, $repo: String!, $name: String!) {
  Tag(name: $name) {
    name
    commit @required {
      repo(owner: $owner, name: $repo) @required {
        owner
        name
      }
    }
  }
}
