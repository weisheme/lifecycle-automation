subscription DeploymentOnK8Container {
  K8Container(state: "running") {
    name
    environment
    timestamp
    image {
      commits @required {
        sha
        repo {
          name
          owner
        }
      }
    }
  }
}
