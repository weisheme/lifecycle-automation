query GitHubId($teamId: ID!, $gitHubId: String!) {
  GitHubId(login: $gitHubId) {
    person {
      chatId {
        screenName
        preferences {
            name
            value
        }
        id
        chatTeam(id: $teamId) {
          name
        }
      }
    }
  }
}