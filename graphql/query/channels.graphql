query Channels($teamId: ID!, $first: Int!, $offset: Int!) {
    ChatTeam(id: $teamId) {
        orgs {
            repo(first: $first offset: $offset) {
                name
                channels {
                    name
                }
            }
        }
    }
}